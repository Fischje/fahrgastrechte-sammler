# Optional: Nur erforderlich, wenn die Installation aus einer ZIP statt per git clone erfolgte.
UPDATE_REPOSITORY=https://github.com/DEIN-BENUTZERNAME/fahrgastrechte-sammler.git
UPDATE_BRANCH=main

# Optionaler externer Port
APP_PORT=8080
